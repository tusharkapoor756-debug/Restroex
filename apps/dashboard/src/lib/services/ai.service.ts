import { AiLog } from "../../types";

export class AiService {
  /**
   * Placeholder: Retrieves AI Engine logs.
   * NOTE: Backend endpoint does not exist yet.
   */
  static async listLogs(): Promise<AiLog[]> {
    // throw new Error("Not implemented: Endpoint /ai/logs does not exist.");
    return [];
  }
}
