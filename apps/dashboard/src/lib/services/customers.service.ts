import { Customer } from "../../types";

export class CustomersService {
  /**
   * Placeholder: Retrieves customer list.
   * NOTE: Backend endpoint does not exist yet.
   */
  static async listCustomers(): Promise<Customer[]> {
    // throw new Error("Not implemented: Endpoint /customers does not exist.");
    return [];
  }
}
