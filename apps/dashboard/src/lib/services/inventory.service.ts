import { InventoryItem } from "../../types";

export class InventoryService {
  /**
   * Placeholder: Retrieves inventory list.
   * NOTE: Backend endpoint does not exist yet.
   */
  static async listInventory(): Promise<InventoryItem[]> {
    // throw new Error("Not implemented: Endpoint /inventory does not exist.");
    return [];
  }
}
